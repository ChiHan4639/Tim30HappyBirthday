// 所有可調參數集中在這裡。改完存檔、重新整理就會生效。
export const CONFIG = {
  // 卡片實體尺寸（單位當成公分看就對了：預設 A5 直式 148 x 210 mm）
  // 燈光、光暈、紙紋都會照著卡片對角線自動縮放，所以這裡可以隨便改。
  card: {
    width: 14.8,
    height: 21.0,
    thickness: 0.062,   // 約 0.6mm，一般卡紙厚度
    radius: 0.38,       // 圓角
  },

  // 卡面貼圖解析度（長邊像素）
  faceResolution: 1600,

  // 紙張（蛋殼紋：細緻均勻的美術紙）
  paper: {
    color: '#f3eee4',       // 霧面米白卡紙（偏中性，讓暖光自己去染色）
    edgeColor: '#e4dccd',   // 切口比正面略深
    margin: 0.11,           // 圖四周留白，佔卡片寬度的比例。滿版稿不適用
    roughness: 0.94,
    inkRoughness: 0.80,     // 墨跡比紙面稍微不那麼粗糙
    grainScale: 7,          // 紙纖維顆粒的重複次數（會跟著卡片尺寸縮放）
    bumpScale: 0.55,
    mottle: 0.11,           // 紙面斑駁強度
    roughVar: 0.06,         // 紙紋造成的粗糙度起伏

    // 圖的長寬比跟卡片差在這個範圍內，就當成「已經排好版的滿版稿」：
    // 原樣鋪滿卡片，不裁切也不重新置中。設 0 就是一律裁切置中。
    fullBleedTolerance: 0.06,

    // 局部上光。用 clearcoat 做 —— 一層透明塗層、自己的粗糙度，
    // 用遮罩決定哪裡有，跟實際的 spot UV 上光是同一回事。
    gloss: {
      strength: 1.0,      // 上光區域的塗層強度
      roughness: 0.16,    // 塗層的粗糙度。越小越像鏡面，0.25 以上是霧光
    },
  },

  // 筆跡圖層
  ink: {
    density: 1.7,      // 墨色濃度。1.0 = 完全照原檔的 alpha，越大越濃
  },

  // 燈光
  light: {
    color: 0xffeed6,
    // 以下的位置與強度都是「對 A6 卡片」的基準值，程式會照卡片大小自動換算
    intensity: 720,
    position: [-11, 12, 9.5],   // 燈放近一點，平方反比才會在卡面上拉出明顯的明暗
    angle: 0.72,
    penumbra: 0.75,
    fill: 0.30,        // 極微弱的環境補光，暗面才不會全黑
    rim: 0.20,         // 背側冷色輪廓光
    exposure: 0.92,
    glow: 0.10,        // 卡片後方空氣中的光暈強度（0 = 純黑）
    side: 1.8,         // 右側柔光（面光源）強度
    sideColor: 0xc9cfda,
    env: 0.38,         // 環境反射強度。局部上光要靠它才照得到東西
    noise: 0.045,      // 畫面顆粒。跟著光走：亮的地方明顯，暗部只留一點點去色帶
  },

  // 互動手感（GSAP ScrollSmoother 的模型：輸入改目標，渲染值用時間常數追）
  motion: {
    restX: 0.06,
    restY: -0.28,
    dragSpeed: 0.0095,   // 每 px 轉幾 rad
    smooth: 0.13,        // 秒。實際角度追上目標的時間常數，越大越黏手
    settle: 0.85,        // 秒。回正的時間常數
    friction: 0.94,      // 慣性衰減率（以每 1/60 秒計，已做 fps 無關換算）
    settleBelow: 1.1,    // rad/s。轉得比這慢才開始回正
    maxTiltX: 0.85,
    autoPeriod: 30,      // 自動旋轉模式：幾秒轉一圈
    autoTilt: 0.10,      // 自動旋轉時多一點俯角，看起來像展示而不是平面自轉。
                         // 注意：這擋不住「轉到側面變一條線」—— 平面卡片繞面內的軸轉，
                         // 法線一定會掃過與視線垂直的位置，那一瞬間投影面積就是 0。
                         // 那時看到的是被打亮的切口，是正常的。
    idleDelay: 3.0,      // 幾秒沒動之後開始自己微微漂移
    idleAmount: 0.055,
  },
};
